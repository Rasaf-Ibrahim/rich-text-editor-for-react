---
title: 'Render Rich Text Editor'
---

{/* import */}
import { Callout } from 'nextra-theme-docs'
import { Tab, Tabs } from 'nextra-theme-docs'
import {
    HORIZONTAL_LINE_SPACER___COMPONENT,
    HEADING_TOP_SPACER___COMPONENT, 
    HEADING_BOTTOM_SPACER___COMPONENT
} from '../components/spacer.tsx'



{/* Page Heading */}
### Render Rich Text Editor
<HORIZONTAL_LINE_SPACER___COMPONENT/>


<HEADING_TOP_SPACER___COMPONENT/>

#### Render with Required Props

<HEADING_BOTTOM_SPACER___COMPONENT/>

To render the RichTextEditor component with only the required props, use the following code:


```ts showLineNumbers copy  
// import
//...

export default function Demo() {


  const {output, fetchOutput} = useRichTextEditor()


    return (

        <RichTextEditor

            fetchOutput={fetchOutput}

            toolbarOptions={['clear_format', 'undo', 'redo', 'font', 'header', 'bold', 'italic', 'underline', 'strikethrough', 'text_color', 'highlight_color', 'numbered_list', 'bulleted_list', 'align', 'decrease_indent', 'increase_indent', 'direction', 'blockquote', 'code_block', 'link',  'embed_youtube_video', 'image_base64', 'image_edit', 'sub_script', 'super_script']}

            customizeUI={{
                dark: true,
                primaryColor:'red'
            }}

        />

    )
}
```


<HEADING_TOP_SPACER___COMPONENT/>

#### `fetchOutput`


In line 7, we have used the "useRichTextEditor" hook and destructured  `output` and `fetchOutput`. We are passing the "fetchOutput" as a prop. 

The rich text editor generates HTML and the "output" variable contains the generated HTML.


<HEADING_TOP_SPACER___COMPONENT/>

#### `toolbarOptions`

The "toolbarOptions" prop controls which toolbar options are visible. You can include or exclude specific options based on your requirements. Here are the available options:

```ts copy
'clear_format',
'undo',
'redo', 
'font', 
'header', 
'bold', 
'italic',
'underline',
'strikethrough',
'text_color',
'highlight_color',
'numbered_list', 
'bulleted_list',
'align',
'decrease_indent', 
'increase_indent',
'direction', 
'blockquote', 
'code_block',
'link',
'image_base64',
'image_cloudinary',
'image_edit',
'embed_youtube_video', 
'sub_script', 
'super_script', 
```


<HEADING_TOP_SPACER___COMPONENT/>

#### `customizeUI`

The "customizedUI" prop is not discussed on this page.  This prop is discussed on the [UI Customization](/ui-customization) page.


<HEADING_TOP_SPACER___COMPONENT/>

### Errors which You May Encounter

<HEADING_BOTTOM_SPACER___COMPONENT/>



<Callout type='error' emoji="️🚫">
`ReactTextEditor` cannot be used as a JSX component
</Callout>


Check out this  [link](/common-errors#reacttexteditor-cannot-be-used-as-a-jsx-component) to solve this error.
