---
title: 'How to Import'
---
{/* import */}
import { Tab, Tabs } from 'nextra-theme-docs'
import {
    HORIZONTAL_LINE_SPACER___COMPONENT,
    HEADING_TOP_SPACER___COMPONENT, 
    HEADING_BOTTOM_SPACER___COMPONENT
} from '../components/spacer.tsx'



{/* Page Heading */}
### How to Import
<HORIZONTAL_LINE_SPACER___COMPONENT/>


<Tabs items={['Next.js', 'React+Vite']}>


  <Tab>

   ```ts showLineNumbers copy
        import dynamic from 'next/dynamic'

        const RichTextEditor = dynamic(
            () => import('rich-text-editor-for-react'),
            {
                ssr: false,
                loading: () => <p>Loading...</p>,
            }
        )

        import useRichTextEditor  from 'rich-text-editor-for-react/hook'
   ```

   > Also, you must use the `use client` directive.

  </Tab>



  <Tab>

    ```ts showLineNumbers copy
        import RichTextEditor from 'rich-text-editor-for-react'
        
        import useRichTextEditor  from 'rich-text-editor-for-react/hook'
    ```

    > If you want to use `React.lazy` and `React.Suspense`. You can use them.

  </Tab>

    

</Tabs>

